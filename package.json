{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o css/tailwind.css -c tailwind.config.js --minify",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
